apiVersion: v1
kind: Service
metadata:
  name: tasks-api
  namespace: mcp-poc
  labels:
    app: tasks-api
spec:
  type: NodePort
  selector:
    app: tasks-api
  ports:
    - name: http
      port: 8000
      targetPort: http
      nodePort: 30080
