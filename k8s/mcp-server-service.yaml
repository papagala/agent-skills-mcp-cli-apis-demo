apiVersion: v1
kind: Service
metadata:
  name: mcp-server
  namespace: mcp-poc
  labels:
    app: mcp-server
spec:
  type: NodePort
  selector:
    app: mcp-server
  ports:
    - name: mcp
      port: 8000
      targetPort: mcp
      nodePort: 30090
